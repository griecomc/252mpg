var mpgconvert = function( x ){
	x = x * .425;
	return x;
};

*// Click event *//
// when someone clicks the "convert" button
document.getElementById('clickMe').onclick = function(event) {
	// get the value that they typed into the MPG box
	var mpg = ???;
	// convert it to KPL
	var kpl = mpgconvert( mpg );
	// take that value and enter it into the KPL box
	

}